server {
    listen 80;
    server_name qbittorrent.home.arpa;
    return 301 https://$host:${NGINX_HTTPS_PORT}$request_uri;
}

server {
    listen 443 ssl;
    server_name qbittorrent.home.arpa;

    set $backend qbittorrent:8080;
    include /etc/nginx/proxy_params.conf;
}
