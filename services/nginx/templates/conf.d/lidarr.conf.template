server {
    listen 80;
    server_name lidarr.home.arpa;
    return 301 https://$host:${NGINX_HTTPS_PORT}$request_uri;
}

server {
    listen 443 ssl;
    server_name lidarr.home.arpa;

    set $backend lidarr:8686;
    include /etc/nginx/proxy_params.conf;
}
