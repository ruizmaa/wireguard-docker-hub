server {
    listen 80;
    server_name jellyfin.home.arpa;
    return 301 https://$host:${NGINX_HTTPS_PORT}$request_uri;
}

server {
    listen 443 ssl;
    server_name jellyfin.home.arpa;

    set $backend jellyfin:8096;
    include /etc/nginx/proxy_params.conf;
}
