server {
    listen 80;
    server_name prowlarr.home.arpa;
    return 301 https://$host:${NGINX_HTTPS_PORT}$request_uri;
}

server {
    listen 443 ssl;
    server_name prowlarr.home.arpa;

    set $backend prowlarr:9696;
    set $backend_scheme http;
    include /etc/nginx/proxy_params.conf;
}
