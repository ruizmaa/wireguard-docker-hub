server {
    listen 80;
    server_name dockge.home.arpa;
    return 301 https://$host:${NGINX_HTTPS_PORT}$request_uri;
}

server {
    listen 443 ssl;
    server_name dockge.home.arpa;

    set $backend dockge:5001;
    include /etc/nginx/proxy_params.conf;
}
